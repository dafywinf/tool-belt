# Validation Dashboards

This directory is intentionally minimal. It exists so Grafana can mount a dashboards
directory during validation without the compose stack depending on project-specific dashboards.
